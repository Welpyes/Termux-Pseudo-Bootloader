[selection_title]
distro = Fedora 41 (aarch64)
root = Fedora Root Shell (fallback)
exit = Turn Off Computer

[selection_cmd]
distro = bash $HOME/.config/bootloader/startx
root = bash -c pd sh fedora
exit = bash -c 'pkill -f termux'
